<% @posts.each do |post| %>
  <div class="post">
    <h2><%= link_to post.title, show_post_path(post) %></h2>
    <p><%= post.body %></p>
    <em> by <%= post.user.email %></em>
  </div>
<% end %>

<%= link_to "Create a New Post", new_post_path %>
